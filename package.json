{
  "name": "design-intelligence",
  "version": "0.2.0",
  "private": true,
  "description": "Automotive Design Intelligence content production pipeline with mock data and a static internal viewer.",
  "type": "commonjs",
  "scripts": {
    "collect": "node scripts/collect.js",
    "generate:daily": "node scripts/generate-daily.js",
    "generate:models": "node scripts/generate-model-cards.js",
    "generate:brands": "node scripts/generate-brand-pages.js",
    "generate:social": "node scripts/generate-social-drafts.js",
    "build": "node scripts/build-site.js",
    "pipeline": "npm run collect && npm run generate:daily && npm run generate:models && npm run generate:brands && npm run generate:social && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
